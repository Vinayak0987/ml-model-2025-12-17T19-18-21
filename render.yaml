# Render deployment configuration
services:
  - type: web
    name: ml-model-2025-12-17T19-18-21
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python load_model.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
